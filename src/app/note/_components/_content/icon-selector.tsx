import { css } from '../../../../../styled-system/css';

const icon = css({
  fontSize: 'lg',
});

const IconSelector = () => {
  return <div className={icon}>🍎</div>;
};

export default IconSelector;
